# 🧭 Installation Guide

Follow these steps to install and initialize **this SDK** for your preferred language.

#### 🐍 Python

<pre class="language-bash"><code class="lang-bash"><strong>bash
</strong>
pip install connectify-sdk
</code></pre>

Verify installation:

```bash
python -m connectify --version
```

#### ☕ Java

**For Maven:**

```xml
xml

<dependency>
  <groupId>com.connectify</groupId>
  <artifactId>connectify-sdk</artifactId>
  <version>2.0.0</version>
</dependency>

```

**For Gradle:**

```xml
gradle

implementation 'com.connectify:connectify-sdk:2.0.0'
```

**⚡ JavaScript**

<pre class="language-bash"><code class="lang-bash"><strong>bash
</strong><strong>
</strong><strong>npm install connectify-sdk
</strong></code></pre>

or

```bash
bash

yarn add connectify-sdk
```

Import in your code:

<pre class="language-javascript"><code class="lang-javascript"><strong>javascript.
</strong>
import Connectify from "connectify-sdk";
</code></pre>
