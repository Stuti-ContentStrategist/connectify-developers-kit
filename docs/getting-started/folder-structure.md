# 📂 Folder Structure

Once installation is complete, your project should follow this layout:

```css
project-root/
├── config/
│   └── connectify.json
├── src/
│   ├── main.py / main.js / Main.java
│   └── utils/
│       └── helpers.js
├── logs/
│   └── connectify.log
└── README.md
```

#### 📁 Key Folders

* **config/** – stores SDK and environment configuration files
* **src/** – contains your application source code
* **logs/** – holds activity and error logs
* **README.md** – includes setup and usage details

💡 **Tip:** Always keep credentials and secrets in environment variables, not config files.

***

#### 📚 Next Page

👉 [🔑 First API Call](first-api-call.md)
